name: 'financial_news'
version: '1.0.0'
config-version: 2

profile: 'financial_news'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  financial_news:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
      facts:
        fct_news:
          +tags: ["hourly", "news"]
        fct_economics:
          +tags: ["weekly", "economics"]
      dimensions:
        +tags: ["dimensions"]

flags:
  bigquery_use_batch_source_freshness: True
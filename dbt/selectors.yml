selectors:
  - name: news_pipeline
    description: Runs the news pipeline and its dependencies
    definition: +fct_news
  - name: economics_pipeline
    description: Runs the economics pipeline and its dependencies
    definition: +fct_economics
  - name: dimensions_pipeline
    description: Runs the pipeline for all dimension tables and their dependencies
    definition: +dimensions